name: CI

on:
  push:
    branches: 
      - main
      - 'feature/**'
    paths:
      - '.github/workflows/ci.yml'
      - 'src/**'
      - 'build.zig'
  pull_request:
    branches: 
      - main
      - 'feature/**'
    paths:
      - '.github/workflows/ci.yml'
      - 'src/**'
      - 'build.zig'

jobs:
  build-test:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - uses: mlugg/setup-zig@v2
        with:
          version: 0.15.2

      - name: Run Tests (Debug)
        run: |
          zig build test -Dlibs=false

      - name: Run tests (Release)
        run: | 
          zig build test --release=fast -Dlibs=false
